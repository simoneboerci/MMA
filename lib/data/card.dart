import 'package:mma/data/fund.dart';

class Card extends Fund{

  Card({String name = "Card", String description = "A fund of which we have a physical card", double money = 0}) : super(name, description, money);
}